version: '3.8'

services:
  server:
    build: ./server
    ports:
      - "5001:5001"
    environment:
      - PORT=5001
      - JWT_SECRET=flowforge_secret_key_2024

  client:
    build: ./client
    ports:
      - "3000:3000"
    depends_on:
      - server
